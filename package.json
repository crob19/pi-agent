{
  "name": "pi-agent-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun run --filter @pi-agent/core dev",
    "build": "bun run --filter @pi-agent/core build",
    "start": "bun run --filter @pi-agent/core start"
  }
}
